#!/bin/bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"

just -f "${REPO_ROOT}/justfile" build-fleet-cli >/dev/null
exec "${REPO_ROOT}/server/.fleet/fleetcli" "$@"
